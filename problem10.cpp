#include<iostream>
using namespace std;
int main() {
    float base, height;
    cout << "Enter base: ";
    cin >> base;
    cout << "Enter height: ";
    cin >> height;
    cout << "Area of the triangle: " << 0.5 * base * height << endl;
    return 0;
}
